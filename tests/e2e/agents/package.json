{
	"name": "sstc-agent-hardware-tests",
	"version": "1.0.0",
	"description": "Kiểm tra cho các chuyên gia phần cứng trong SSTC Agent",
	"type": "module",
	"scripts": {
		"test": "jest",
		"test:watch": "jest --watch",
		"test:coverage": "jest --coverage"
	},
	"devDependencies": {
		"@types/jest": "^29.5.0",
		"jest": "^29.7.0",
		"ts-jest": "^29.1.0"
	}
}
