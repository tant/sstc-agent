{
  "name": "sstc-agent-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for SSTC Agent",
  "scripts": {
    "test": "jest",
    "test:e2e": "jest e2e --runInBand",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:debug": "node --inspect-brk -r ts-node/register node_modules/.bin/jest --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^24.0.0",
    "axios": "^1.6.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}